% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Go_renderReport.R
\name{Go_renderReport}
\alias{Go_renderReport}
\title{Go_renderReport - Render the HTML summary report}
\usage{
Go_renderReport(
  family,
  project,
  expInfo,
  dirInfo,
  tabInfo,
  imgInfo,
  sumInfo,
  output_dir,
  logo_path = NULL,
  cc_email = NULL
)
}
\arguments{
\item{family}{One of "16S", "MG", "RNAseq".}

\item{project}{Project name, used in the output HTML filename.}

\item{expInfo, dirInfo, tabInfo, imgInfo, sumInfo}{Report objects built with
the matching \code{Go_*Info()} functions.}

\item{output_dir}{Directory the rendered HTML is written into. Created if
it does not exist.}

\item{logo_path}{Optional path to a logo image shown at the bottom of the
report. Omitted (\code{NULL}, the default) skips the logo section
entirely — no logo image is bundled with Gotools.}

\item{cc_email}{Optional email address CC'd on the report's built-in
"leave a comment" mailto link. Omitted (\code{NULL}, the default) leaves
the CC field out of that link.}
}
\value{
Invisibly, the path to the rendered HTML file.
}
\description{
Renders the bundled 16S/MG/RNAseq Rmd report template into a single-file
HTML summary report, using the report objects produced by
\code{Go_expInfo()}, \code{Go_dirInfo()}, \code{Go_tabInfo()},
\code{Go_imgInfo()}, and \code{Go_sumInfo()}.
}
