% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Go_hybridBoxTrajectory.R
\name{Go_hybridBoxTrajectory}
\alias{Go_hybridBoxTrajectory}
\title{Hybrid Boxplot + Trajectory Plot for Repeated Measures}
\usage{
Go_hybridBoxTrajectory(
  df = NULL,
  psIN = NULL,
  project,
  outcomes,
  ref.var,
  traj.var,
  orders = NULL,
  time.var,
  subject.var,
  name = NULL,
  mycols = NULL,
  statistics = TRUE,
  p_adjust = TRUE,
  log_transform = c("auto", "none", "log1p"),
  emmeans_at = NULL,
  span = 0.85,
  loess_min_n = 5,
  lm_min_n = 3,
  width = 8,
  height = NULL
)
}
\arguments{
\item{df}{Data frame containing the variables to plot. Provide either
\code{df} or \code{psIN}.}

\item{psIN}{Phyloseq object. In the current version, \code{psIN} mode
supports alpha-diversity outcomes and internally calls \code{Go_adiv()}.}

\item{project}{Project name used for output folder and file naming.}

\item{outcomes}{Numeric outcomes to plot. When \code{psIN} is provided,
these should be alpha-diversity metrics such as \code{"Shannon"} or
\code{"Chao1"}.}

\item{ref.var}{Grouping variable used for the left reference boxplot.}

\item{traj.var}{Grouping variable used for the right trajectory panel.}

\item{orders}{Optional ordering specification for the grouping variables.
For this function, provide a named list such as
\code{list(ref = c("NC", "CD"), traj = c("Neg", "Mild", "Mod"))}.
The \code{ref} entry controls the left reference panel order and filtering,
and the \code{traj} entry controls the right trajectory panel order and filtering.}

\item{time.var}{Numeric time variable for the trajectory x-axis.}

\item{subject.var}{Subject ID used for repeated-measures lines and the
random intercept in the LMM.}

\item{name}{Optional name tag appended to output files.}

\item{mycols}{Optional named color vector. Names should cover all values in
\code{ref.levels} and \code{traj.levels}.}

\item{statistics}{Logical; if \code{TRUE}, fit an LMM for each outcome in
the trajectory panel and export ANOVA / emmeans tables.}

\item{p_adjust}{Logical; if \code{TRUE}, use BH adjustment for pairwise
\code{emmeans} contrasts and for the displayed within-model FDR label.}

\item{log_transform}{One of \code{"auto"}, \code{"none"}, or \code{"log1p"}.
In \code{"auto"} mode, richness-like outcomes such as Chao1, ACE, and
Observed use \code{log1p()} in the LMM.}

\item{emmeans_at}{Optional named list passed to \code{emmeans(..., at = ...)}.
If \code{NULL} and \code{time.var} is numeric, group means are estimated at
the median observed time.}

\item{span}{LOESS span for the colored smooth trajectory.}

\item{loess_min_n}{Minimum observations per group for LOESS overlay.}

\item{lm_min_n}{Minimum observations per group for dashed LM overlay.}

\item{width}{Output PDF width in inches.}

\item{height}{Output PDF height in inches. If \code{NULL}, height is set from
the number of outcomes.}
}
\value{
Invisibly returns a list containing plots, fitted model summaries,
and output paths.
}
\description{
Builds a hybrid figure with a reference boxplot panel on the left and a
repeated-measures trajectory panel on the right for one or more outcomes.
The trajectory panel is summarized with a linear mixed model (LMM) and
optional \code{emmeans} contrasts.
}
\examples{
\dontrun{
Go_hybridBoxTrajectory(
  df = dat,
  project = "MAPS",
  outcomes = c("Chao1", "Shannon"),
  ref.var = "TruncGroup",
  traj.var = "TruncITxOnly",
  orders = list(ref = c("NC", "CD"), traj = c("Neg", "Mild", "Mod")),
  time.var = "POD_num",
  subject.var = "subject_id"
)
}

}
