% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Go_OR_fit.R
\name{Go_OR_fit}
\alias{Go_OR_fit}
\title{Go_OR_fit}
\usage{
Go_OR_fit(
  psIN,
  outcome,
  pos_class,
  taxrank = "Species",
  covars = NULL,
  transform = c("log_rel", "relative", "count"),
  top_n = 15,
  prevalence_min = 0,
  abundance_min = 0,
  project = NULL,
  name = NULL,
  seed = 1234,
  na_action = c("complete"),
  verbose = TRUE
)
}
\arguments{
\item{psIN}{A \code{phyloseq} object.}

\item{outcome}{Character scalar; outcome column in \code{sample_data(psIN)}.}

\item{pos_class}{Character or numeric scalar defining the positive class.}

\item{taxrank}{Character taxonomic rank. Defaults to \code{"Species"}.
Use \code{"ASV"} to keep the original feature level.}

\item{covars}{Optional character vector of adjustment covariates.}

\item{transform}{One of \code{"log_rel"}, \code{"relative"}, or
\code{"count"}.}

\item{top_n}{Optional integer; keep the top N taxa by mean transformed
abundance before fitting models. Defaults to \code{15}.}

\item{prevalence_min}{Optional prevalence filter on relative abundance
presence rate. Defaults to \code{0}.}

\item{abundance_min}{Optional mean relative abundance filter. Defaults to
\code{0}.}

\item{project}{Optional project name used to save the fitted object.}

\item{name}{Optional file tag for saving the fitted object. When both
\code{project} and \code{name} are provided, the fitted object is saved to
\code{<project>_YYMMDD/table/OR/}.}

\item{seed}{Integer seed stored for reproducibility and applied before model
fitting. Defaults to \code{1234}.}

\item{na_action}{Currently only \code{"complete"} is supported.}

\item{verbose}{Logical; print progress messages.}
}
\value{
A list of class \code{"Go_OR_fit"}.
}
\description{
For a binary outcome defined in \code{sample_data(psIN)}, this function fits
one logistic regression model per microbial feature after optional taxonomic
agglomeration and abundance transformation. Each model estimates the adjusted
odds ratio for one taxon while controlling for user-specified covariates.
}
\details{
Fit taxon-wise adjusted logistic regression models from a phyloseq object.
}
