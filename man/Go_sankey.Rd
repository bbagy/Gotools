% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Go_sankey.R, R/Go_sankey.dev.R
\name{Go_sankey}
\alias{Go_sankey}
\title{Generate sankey plots for feature or transition data}
\usage{
Go_sankey(
  project,
  SigASVs = NULL,
  map = NULL,
  targets.bac = NULL,
  target.rank = NULL,
  outcome = NULL,
  column1 = NULL,
  column2 = NULL,
  Addcolumn = NULL,
  data = NULL,
  mode = c("feature", "transition"),
  axes = NULL,
  fill_var = NULL,
  facet_var = NULL,
  weight_var = NULL,
  orders = NULL,
  name = NULL,
  height = 4,
  width = 6,
  plotCols = 2,
  plotRows = 1,
  size_by = c("abundance", "count"),
  alpha = 0.72,
  mycol = NULL
)

Go_sankey(
  project,
  SigASVs = NULL,
  map = NULL,
  targets.bac = NULL,
  target.rank = NULL,
  outcome = NULL,
  column1 = NULL,
  column2 = NULL,
  Addcolumn = NULL,
  data = NULL,
  mode = c("feature", "transition"),
  axes = NULL,
  fill_var = NULL,
  facet_var = NULL,
  weight_var = NULL,
  orders = NULL,
  name = NULL,
  height = 4,
  width = 6,
  plotCols = 2,
  plotRows = 1,
  size_by = c("abundance", "count"),
  alpha = 0.72,
  mycol = NULL
)
}
\arguments{
\item{project}{Project name used for output directory and file naming.}

\item{SigASVs}{Either a CSV path or a data frame containing significant
features and taxonomy columns. Used in \code{mode = "feature"}.}

\item{map}{Either a CSV path or a data frame containing sample metadata.
Used in \code{mode = "feature"}.}

\item{targets.bac}{Character vector of taxa to plot. Used in
\code{mode = "feature"}.}

\item{target.rank}{Taxonomy column used to match \code{targets.bac}.}

\item{outcome}{Primary metadata variable shown on the last axis in
\code{mode = "feature"}.}

\item{column1}{Optional intermediate metadata variable in
\code{mode = "feature"}.}

\item{column2}{Optional second intermediate metadata variable in
\code{mode = "feature"}.}

\item{Addcolumn}{Optional extra metadata columns. When provided in
\code{mode = "feature"}, these are inserted before \code{outcome}.}

\item{data}{Either a CSV path or a data frame containing wide-format
transition data. Used in \code{mode = "transition"}.}

\item{mode}{One of \code{"feature"} or \code{"transition"}.}

\item{axes}{Character vector of ordered axis columns for
\code{mode = "transition"}, for example \code{c("V1","V2","V3")}.}

\item{fill_var}{Optional column used for flow fill in
\code{mode = "transition"}.}

\item{facet_var}{Optional faceting column in \code{mode = "transition"}.}

\item{weight_var}{Optional weight column in \code{mode = "transition"}.
When NULL, paths are counted.}

\item{orders}{Optional level order vector applied to categorical axes.}

\item{name}{Optional label appended to the output file name.}

\item{height}{Height of the output PDF.}

\item{width}{Width of the output PDF.}

\item{plotCols}{Number of columns for multi-panel layout.}

\item{plotRows}{Number of rows for multi-panel layout.}

\item{size_by}{Whether ribbon size should reflect summed abundance or
sample count per path in \code{mode = "feature"}. One of
\code{"abundance"} or \code{"count"}.}

\item{alpha}{Flow transparency.}

\item{mycol}{Optional color vector. In \code{mode = "feature"} it is
mapped to features. In \code{mode = "transition"} it is mapped to
fill groups.}
}
\value{
Invisibly returns a list of ggplot objects. A PDF is written to
the dated project \code{pdf/} directory.

Invisibly returns a list of ggplot objects. A PDF is written to
the dated project \code{pdf/} directory.
}
\description{
Uses \code{ggsankey::geom_sankey()} to draw sankey plots from either
feature tables plus metadata or from wide-format transition tables.
\itemize{
\item \code{mode = "feature"} keeps the original SigASVs + metadata
workflow for taxa-centered sankey plots.
\item \code{mode = "transition"} draws transition sankey plots directly
from a wide-format table such as \code{V1 -> V2 -> V3} states.
}

Uses \code{ggsankey::geom_sankey()} to draw sankey plots from either
feature tables plus metadata or from wide-format transition tables.
\itemize{
\item \code{mode = "feature"} keeps the original SigASVs + metadata
workflow for taxa-centered sankey plots.
\item \code{mode = "transition"} draws transition sankey plots directly
from a wide-format table such as \code{V1 -> V2 -> V3} states.
}
}
