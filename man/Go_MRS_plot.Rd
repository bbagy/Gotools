% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Go_MRS_plot.R
\name{Go_MRS_plot}
\alias{Go_MRS_plot}
\title{Go_MRS_plot}
\usage{
Go_MRS_plot(
  fit,
  plot_type = c("all", "score", "roc", "coef"),
  top_n = 20,
  title = NULL,
  style = c("auto", "default", "paper"),
  project = NULL,
  name = NULL,
  order = NULL,
  mycol = c("#1f77b4", "#d62728"),
  patchwork = FALSE
)
}
\arguments{
\item{fit}{A \code{Go_MRS_fit()} result object.}

\item{plot_type}{One of \code{"score"}, \code{"roc"}, or \code{"coef"}.}

\item{top_n}{Integer; number of coefficients to display for
\code{plot_type = "coef"}.}

\item{title}{Optional custom title.}

\item{style}{One of \code{"auto"}, \code{"default"}, or \code{"paper"}.}

\item{project}{Optional project name used for automatic output directory
creation when saving.}

\item{name}{Optional filename tag appended to saved PDFs.}

\item{order}{Optional character vector controlling group order in binary
score plots. Must have length 2 when provided.}

\item{mycol}{Optional color vector. The first color is used for the first
group (or negative direction), and the second for the second group (or
positive direction).}

\item{patchwork}{Logical. If \code{TRUE}, skip saving and return the plot object(s) for use with \code{Gg_patchwork()} or the \pkg{patchwork} package. Default \code{FALSE}.}
}
\value{
A ggplot object. The returned plot may carry attributes such as
\code{recommended_width}, \code{recommended_height}, \code{n_grp}, and
\code{max_lbl_chars} for downstream PDF sizing. The output path is stored
in \code{attr(plot, "saved_path")}.
the output path is stored in \code{attr(plot, "saved_path")}.
}
\description{
Draws standardized plots from a \code{Go_MRS_fit()} object. The plot subtitle
automatically includes the recorded model engine, family, outcome type,
random-effect status, feature count, and summary metric.
}
\details{
Plot helper for \code{Go_MRS_fit()} results.
}
