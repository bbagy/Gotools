% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Go_mixture_V1.R
\name{Go_mixture}
\alias{Go_mixture}
\title{Mixture Analysis: BKMR + QGCOMP}
\usage{
Go_mixture(
  psIN,
  project,
  mix_vars,
  outcomes,
  covariates = NULL,
  bkmr_iter = 5000,
  seed = 42,
  outcome_family = "auto",
  cluster_var = NULL,
  qgcomp_boot = FALSE,
  qgcomp_B = 200,
  response_max_panels = 15,
  response_ncol = 3,
  response_width = 8,
  response_height = 11,
  response_panel_width = 2.1,
  response_panel_height = 1.75,
  mycols = c("#74ADD1", "#E31A1C"),
  pip_threshold = 0.5,
  panel_width = 2.5,
  font = 10,
  name = NULL
)
}
\arguments{
\item{psIN}{Phyloseq object. OTU table provides microbiome/pathway feature
values; sample_data provides outcomes and covariates.}

\item{project}{Project name string.}

\item{mix_vars}{Character vector of mixture variable names. Can be any
combination of: ASV_IDs (tax_table$ASV_ID), KO/PWY names (taxa_names),
or sample_data column names (e.g. metals, clinical variables).}

\item{outcomes}{Character vector of outcome variable names in sample_data.
Required.}

\item{covariates}{Character vector of covariate names in sample_data, or NULL.}

\item{bkmr_iter}{Integer. MCMC iterations for BKMR (default 5000).}

\item{seed}{Integer. Random seed.}

\item{outcome_family}{"auto", "gaussian", or "binomial". In auto mode,
two-level factor/character/logical outcomes and 0/1 numeric outcomes use
binomial models; all others use gaussian models.}

\item{cluster_var}{Optional sample_data column used as qgcomp bootstrap id
for repeated observations, e.g. subject ID.}

\item{qgcomp_boot}{Logical. If TRUE, use qgcomp.glm.boot(); if FALSE, use
qgcomp.noboot(). Automatically set to TRUE when cluster_var is provided.}

\item{qgcomp_B}{Integer. Bootstrap replicates for qgcomp.glm.boot().}

\item{response_max_panels}{Maximum BKMR response panels per PDF page.}

\item{response_ncol}{Number of columns for BKMR response PDFs.}

\item{response_width}{Optional fixed width in inches for BKMR response PDFs.
If NULL, width is calculated from response_panel_width and response_ncol.}

\item{response_height}{Optional fixed height in inches for BKMR response PDFs.
If NULL, height is calculated from response_panel_height and row count.}

\item{response_panel_width}{Width in inches for each BKMR response facet panel.}

\item{response_panel_height}{Height in inches for each BKMR response facet panel.}

\item{mycols}{Length-2 color vector: \verb{[1]} negative, \verb{[2]} positive.}

\item{pip_threshold}{PIP cutoff for univariate response curves (default 0.5).}

\item{panel_width}{Fixed panel width in inches (y-label area auto-expands;
see GOTOOLS_PLOT_CONVENTIONS.md).}

\item{font}{Base font size in points.}

\item{name}{Optional report token from ensure_report_token().}
}
\value{
Invisible named list: \code{results[[outcome]]$bkmr}, \verb{$bkmr_response},
and \verb{$qgcomp} data frames.
}
\description{
Models how a mixture of variables predicts continuous outcomes using
Bayesian Kernel Machine Regression (BKMR) and Quantile G-computation (QGCOMP).
}
\details{
mix_vars sources (auto-detected in order):
\enumerate{
\item sample_data columns  → used directly, scaled
\item tax_table$ASV_ID     → CLR-transformed from OTU table (16S)
\item taxa_names(psIN)     → CLR-transformed from OTU table (KO, PWY, pathway)
}

Both BKMR and QGCOMP are always run for every outcome.
}
