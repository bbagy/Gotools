% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Go_boxplot_V10.R
\name{Go_boxplot}
\alias{Go_boxplot}
\title{Generate Boxplots for Multiple Variables}
\usage{
Go_boxplot(
  df = NULL,
  psIN = NULL,
  cate.vars,
  project,
  outcomes,
  orders = NULL,
  mycols = NULL,
  combination = NULL,
  chao1_ylim = NULL,
  shannon_ylim = NULL,
  title = NULL,
  facet = NULL,
  paired = NULL,
  name = NULL,
  ncol = NULL,
  addnumber = TRUE,
  statistics = TRUE,
  covariates = NULL,
  p_adjust = TRUE,
  cutoff = 0.1,
  min_height = 2,
  min_width = 2,
  plotCols = 1,
  plotRows = 1,
  patchwork = FALSE
)
}
\arguments{
\item{df}{Data frame containing the data to be plotted. Provide either \code{df} or \code{psIN}.}

\item{psIN}{Phyloseq object. If \code{outcomes} are alpha diversity metrics (e.g. "Shannon"),
\code{Go_adiv()} is called internally. If taxa names are given, rank is auto-detected from
\code{tax_table}.}

\item{cate.vars}{One categorical variable to be used for the boxplot x-axis.}

\item{project}{Project name used for output file naming.}

\item{outcomes}{Numeric variables (or alpha metrics / taxa names when using psIN) to plot on y-axis.}

\item{orders}{Vector of ordered factor levels for the categorical variables.}

\item{mycols}{Custom color palette for the plots.}

\item{combination}{Number of group combinations to display in the boxplot.}

\item{chao1_ylim}{Optional maximum y-axis value for \code{Chao1} plots. The lower bound is fixed at 0.
Only applied when the outcome is \code{"Chao1"}. If \code{NULL}, y-axis limits are determined automatically.}

\item{shannon_ylim}{Optional maximum y-axis value for \code{Shannon} plots. The lower bound is fixed at 0.
Only applied when the outcome is \code{"Shannon"}. If \code{NULL}, y-axis limits are determined automatically.}

\item{title}{Title of the plot.}

\item{facet}{Optional variable for creating facetted plots.}

\item{paired}{Indicates if the data points are paired.}

\item{name}{Optional name for saving the plot.}

\item{ncol}{Number of columns for facet wrapping in the plot.}

\item{addnumber}{Boolean to add the number of samples in each group. Default TRUE.}

\item{statistics}{Whether to perform statistical tests. Default TRUE.}

\item{covariates}{Optional covariate column names for adjusted models (ANCOVA/LMM).}

\item{p_adjust}{Logical. Whether to apply BH adjustment to pairwise p-values. Default TRUE.}

\item{cutoff}{Significance level for statistical tests. Default 0.1.}

\item{min_height}{Minimum plot panel height in inches (excluding x-axis labels). Default 2.}

\item{min_width}{Minimum plot width per column in inches. Default 2.}

\item{plotCols}{Number of columns in the multiplot layout. Default 1.}

\item{plotRows}{Number of rows in the multiplot layout. Default 1.}

\item{patchwork}{Logical. If \code{TRUE}, skip saving and return the plot object(s) for use with \code{Gg_patchwork()} or the \pkg{patchwork} package. Default \code{FALSE}.}
}
\value{
A PDF file containing the boxplots.
}
\description{
Generate Boxplots for Multiple Variables
}
\details{
PDF dimensions are calculated automatically from the data:
\itemize{
\item \strong{Width}: \code{max(min_width, n_groups * 0.4) * plotCols}
\item \strong{Panel height}: \code{max(min_height, 2 + n_comparisons * 0.3) * plotRows}
\item \strong{Label height}: \code{max(0.5, max_label_chars * 0.09)} added for rotated x-axis labels
}
Panel height and label height are allocated separately via \code{cowplot::plot_grid()},
so long group names do not eat into the boxplot panel space.
}
\examples{
Go_boxplot(df = my_data, cate.vars = "Group", project = "MyProject",
           outcomes = c("Variable1", "Variable2"))

Go_boxplot(psIN = ps, cate.vars = "Group", project = "MyProject",
           outcomes = c("Shannon", "Chao1"))

}
