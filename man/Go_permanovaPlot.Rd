% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Go_permanovaPlot.R
\name{Go_permanovaPlot}
\alias{Go_permanovaPlot}
\title{PERMANOVA R² bar chart with optional inset summary}
\usage{
Go_permanovaPlot(
  psIN,
  project,
  vars = NULL,
  category_map = NULL,
  distance_metric = "bray",
  name = NULL,
  permutations = 999,
  p_adjust = "BH",
  min_n = 3,
  max_levels = 12,
  include_numeric = TRUE,
  include_character = TRUE,
  cat_colors = NULL,
  show_inset = TRUE,
  inset_left = 0.58,
  inset_bottom = 0.34,
  inset_right = 0.99,
  inset_top = 0.73,
  show_strip_text = FALSE,
  base_size = 10,
  height = NULL,
  width = NULL
)
}
\arguments{
\item{psIN}{Phyloseq object.}

\item{project}{Project name used by \code{Go_path()} to build output directories.}

\item{vars}{Character vector of metadata variable names to screen. If \code{NULL}
(default), variables are detected automatically from \code{sample_data(psIN)}.}

\item{category_map}{Optional named list mapping category labels (character)
to vectors of variable names, e.g.
\code{list("Clinical" = c("BMI", "Age"), "Treatment" = c("Group"))}.
Variables not present in the map are labelled \code{"Metadata"}.}

\item{distance_metric}{Distance metric passed to \code{phyloseq::distance()}.
Default \code{"bray"}.}

\item{name}{Optional extra suffix appended to output file names.}

\item{permutations}{Number of permutations for \code{vegan::adonis2()}.
Default \code{999}.}

\item{p_adjust}{P-value adjustment method passed to \code{stats::p.adjust()}.
Default \code{"BH"}.}

\item{min_n}{Minimum number of complete-case samples required to run
PERMANOVA for a variable. Default \code{3}.}

\item{max_levels}{Maximum number of factor levels allowed when
auto-detecting categorical variables. Default \code{12}.}

\item{include_numeric}{Include numeric metadata columns in auto-detection.
Default \code{TRUE}.}

\item{include_character}{Include factor/character/logical metadata columns
in auto-detection. Default \code{TRUE}.}

\item{cat_colors}{Optional named character vector of hex colours, one per
category. If \code{NULL} (default), a built-in palette is used.}

\item{show_inset}{Logical. Whether to embed the per-category lollipop inset.
Default \code{TRUE}.}

\item{inset_left, inset_bottom, inset_right, inset_top}{Fractional coordinates
(0–1) that define the inset bounding box within the main panel.
Defaults place the inset at roughly the upper-right quadrant
(\code{left=0.58}, \code{bottom=0.34}, \code{right=0.99}, \code{top=0.73}).}

\item{show_strip_text}{Logical. Whether to display facet strip labels.
Default \code{FALSE} (strips are hidden; category identity is conveyed by fill
colour and legend).}

\item{base_size}{Base font size (pt) for \code{ggplot2::theme_classic()}.
Default \code{10}.}

\item{height}{Height of the saved PDF in inches. If \code{NULL}, height is
determined automatically from the number of variables.}

\item{width}{Width of the saved PDF in inches. If \code{NULL}, width is
determined automatically from the longest variable name.}
}
\value{
Invisibly returns a named list:
\describe{
\item{\code{results}}{\code{data.frame} with one row per variable containing
\code{variable}, \code{category}, \code{type}, \code{n}, \code{levels}, \code{R2}, \code{pval},
\code{p_adj}, \code{sig}.}
\item{\code{plot_main}}{\code{ggplot} object – the main bar chart.}
\item{\code{plot_inset}}{\code{ggplot} object – the lollipop inset (\code{NULL} when
\code{show_inset = FALSE}).}
\item{\code{plot_combined}}{Final combined plot written to PDF.}
\item{\code{csv}}{Path to the CSV results file.}
\item{\code{pdf}}{Path to the PDF figure file.}
}
}
\description{
Runs one-variable PERMANOVA (adonis2) for each metadata variable against a
beta-diversity distance matrix, then produces a publication-ready figure
consisting of:
}
\details{
\itemize{
\item \strong{Main panel} – horizontal bar chart of R² (\%) per variable, faceted by
category and sorted within each facet.
\item \strong{Inset panel} (optional) – lollipop chart of mean R² per category,
embedded in the upper-right corner of the main panel via
\code{patchwork::inset_element()}.
}

A CSV table of all PERMANOVA results is also written to disk alongside the
PDF figure.
}
\examples{
\dontrun{
Go_permanovaPlot(
  psIN = ps,
  project = "DemoProject",
  distance_metric = "bray",
  category_map = list(
    "Demographic & Lifestyle" = c("age", "sex", "BMI", "smoking"),
    "Periodontal Status"      = c("periodontitis_severity", "Mean_PD"),
    "Cognitive Function"      = c("cognitive_status", "MMSE"),
    "Medical History"         = c("hypertension", "diabetes")
  )
)
}

}
